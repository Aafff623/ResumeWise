const qs=(s,r=document)=>r.querySelector(s);const qsa=(s,r=document)=>[...r.querySelectorAll(s)];
function toast(msg){const el=qs('#toast');if(!el)return;el.textContent=msg;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),1700)}
qsa('[data-job]').forEach(el=>el.addEventListener('click',()=>{qsa('[data-job]').forEach(x=>x.classList.remove('active'));el.classList.add('active');localStorage.setItem('rw-job',el.dataset.job);toast('目标岗位已切换')}));
const input=qs('#resumeText');if(input){const cached=localStorage.getItem('rw-resume');if(cached)input.value=cached;input.addEventListener('input',()=>{localStorage.setItem('rw-resume',input.value);const counter=qs('#charCount');if(counter)counter.textContent=`约 ${input.value.length} 字 · 已缓存到 localStorage`});qsa('[data-sample]').forEach(btn=>btn.addEventListener('click',()=>{input.value=qs('#sampleResume').textContent.trim();input.dispatchEvent(new Event('input'));qsa('[data-sample]').forEach(x=>x.classList.toggle('active',x===btn));toast('已填充示例简历')}))}
qsa('[data-loading-link]').forEach(btn=>btn.addEventListener('click',e=>{e.preventDefault();const overlay=qs('#loading');overlay?.classList.add('show');setTimeout(()=>location.href=btn.getAttribute('href'),900)}));
qsa('[data-copy]').forEach(btn=>btn.addEventListener('click',async()=>{const target=qs(btn.dataset.copy);try{await navigator.clipboard.writeText(target.innerText);toast('优化简历已复制')}catch{toast('当前浏览器限制复制，请手动选择文本')}}));
qsa('[data-download]').forEach(btn=>btn.addEventListener('click',()=>{const target=qs(btn.dataset.download);const blob=new Blob([target.innerText],{type:'text/plain;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='ResumeWise-optimized-resume.txt';a.click();URL.revokeObjectURL(a.href);toast('已生成 TXT 文件')}));
