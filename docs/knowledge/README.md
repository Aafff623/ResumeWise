# knowledge/ — 可迁移知识

本目录存放**跨项目可复用**的经验与方法论，不放本项目特定的产品决策（那些在 `CONTEXT.md` / `docs/adr/`）。

## 写入规则

- **仅用户要求时**写入；禁止 Agent 未经允许自动沉淀。
- 写入流程：草稿 → 用户 Review → 定稿。
- 每篇应有明确的「可迁移到什么项目」边界。

## 已收录

_（初始化阶段暂无。后续如「Spring Boot 接大模型的 Prompt 校验套路」「Vue3 + Element Plus 评分卡组件模式」等经验成熟后，按用户要求沉淀。）_
